{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "Desktop-only auto-update permissions (updater + process). Scoped to desktop platforms because the updater/process plugins are not present in the mobile build.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:default"]
}
